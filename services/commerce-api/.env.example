# commerce-api uses the env at the repo root (../../.env).
# This file is here as a reference for which variables this service needs.
# Do not commit a real .env. Use the root .env.example as the template.

# See ../../.env.example for the full list and comments.
